# Deployment
PUBLIC_ORIGIN=https://prikkert.nl
PUBLIC_NO_INDEX=1

# Cookies
COOKIE_PREFIX=prikkert

# Database
POSTGRES_USER=prikkert
POSTGRES_PASSWORD=
POSTGRES_DB=prikkert
DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}